#include<iostream>
#include<vector>

using namespace std;

int main() {
	int N = 0;
	int M = 0;

	cin >> N;
	if (N > 1000 || N < 0) { return 0; }

	cin >> M;
	if (M < 0 || M>10000) { return 0; }

	vector<vector<int>>

		return 0;
}